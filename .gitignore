# Secrets & credentials
.env
.env.*
!.env.example
.auth/
tampermonkey-script.md
Makefile.local

# Docker compose (real deployments — use .example versions as templates)
docker-compose.dev.yml
docker-compose.komodo.yml

# Node dependencies
node_modules/

# Build output
dist/
build/

# IDE / session history
.specstory/
.claude/
