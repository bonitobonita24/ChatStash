# Secrets & credentials
.env
.env.*
!.env.example
.auth/
tampermonkey-script.md
Makefile.local

# Docker compose (local overrides — use .example as template)
docker-compose.dev.yml
docker-compose.staging.yml
docker-compose.komodo.yml

# Node dependencies
node_modules/

# Build output
dist/
build/

# IDE / session history
.specstory/
.claude/
